{
  "name": "submerger-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E smoke tests for the SubMerger backend + built SPA",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
